upstream nodejs {
  server web:4201;
}

server {
  listen 80;

  # Set the default locale of the Angular web app.
  set $default_locale sv;

  # Enable sending precompressed ".gz" versions of static files if such exist.
  # The precompressed files are created when the app is built.
  # https://nginx.org/en/docs/http/ngx_http_gzip_static_module.html
  gzip_static on;
  # Configure compression of proxied requests.
  # https://nginx.org/en/docs/http/ngx_http_gzip_module.html#gzip_proxied
  gzip_proxied no-cache no-store private expired auth;

  # Uncomment the gzip directives below to enable on-the-fly gzip compression
  # of html dynamically generated by server-side rendering. Enabling will
  # increase server load, but significantly reduce the size of transfered html
  # files, even on the lowest compression level (1).
  # https://nginx.org/en/docs/http/ngx_http_gzip_module.html
  #gzip on;
  #gzip_comp_level 1;
  #gzip_types text/html;

  root /static;

  # === 1. Generic cache control blocks for static files ===

  # Unprefixed special files at the root
  location ~ ^/(robots\.txt|sitemap\.txt)$ {
    expires -1;
    add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate";
    try_files /$default_locale$uri =404;
  }

  # Unversioned static files in folders: /<locale>/assets/, /<locale>/static-html/, /<locale>/svg/
  location ~ ^/[^/]+/(assets|static-html|svg)/ {
    expires 1d;
    add_header Cache-Control "public";
    try_files $uri =404;
  }

  # Long-term caching for hashed files: /<locale>/*.js, .css, fonts
  location ~ ^/[^/]+/[^/]+\.(js|woff2?|ttf|css|otf|eot)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
    try_files $uri =404;
  }

  # === 2. Try serving file directly, or fall back to SSR backend ===

  location / {
    try_files $uri /$default_locale$uri @backend;
  }

  location @backend {
    proxy_pass http://nodejs;

    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $http_host;
    proxy_http_version 1.1;
    proxy_set_header X-NginX-Proxy true;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_cache_bypass $http_upgrade;
    proxy_redirect off;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_buffers 24 16k; # total buffer space = 384 KB per request
    proxy_buffer_size 16k; # buffer size for headers
  }
}
