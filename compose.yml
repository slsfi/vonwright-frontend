services:
  nginx:
    image: docker.io/library/nginx:1.28.2
    depends_on:
      - web
    ports:
      - 2090:80
    restart: unless-stopped
    volumes:
      - browser-static:/static
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/slsfi/vonwright-frontend:2.6.1-production.1
    restart: unless-stopped
    volumes:
      - browser-static:/digital-edition-frontend-ng/dist/app/browser
volumes:
  browser-static:
